import { SenegalPhone } from './senegal-phone';

describe('SenegalPhone', () => {
  it('should create an instance', () => {
    const directive = new SenegalPhone();
    expect(directive).toBeTruthy();
  });
});
